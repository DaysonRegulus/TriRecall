// Enum to represent the available sorting options.
enum TopicSortOption {
  mostRecent,
  oldest,
  dueSoonest,
}

// Enum to represent the available filtering options.
enum TopicFilterOption {
  all,
  active,
  mastered,
}